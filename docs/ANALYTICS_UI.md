# Analytics UI vs telemetry

**Analytics page** = interview results, coach cards, charts, history search/bookmarks.

**Diagnostics** (Profile) = anonymous local telemetry aggregates + JSON export ([`services/telemetry.ts`](../services/telemetry.ts)).

No hosted analytics by default.
